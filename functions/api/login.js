import {sign} from './_auth.js';
export async function onRequestPost({request,env}){const {username,password}=await request.json();if(username!==env.ADMIN_USER||password!==env.ADMIN_PASSWORD)return new Response(JSON.stringify({ok:false}),{status:401,headers:{'content-type':'application/json'}});const exp=Date.now()+30*24*3600*1000,payload=`${username}|${exp}`,sig=await sign(payload,env.SESSION_SECRET),token=btoa(payload)+'|'+sig;return new Response(JSON.stringify({ok:true}),{headers:{'content-type':'application/json','set-cookie':`tr_session=${encodeURIComponent(token)}; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=2592000`}})}
